- 上下文管理器：管理资源（数据库连接，锁，文件）的获取与释放，在即使遇到异常的情况下，资源也能被正确清理，上下文管理器只是一个协议，核心是实现了_ _ enter_ _() 和 _ _ exit _ _()魔法方法
- try-finally =>with .. as ..
- 自定义上下文管理器
  - 类：含有_ _enter _ _()，_ _ _next_ _()  【**实现一个文件对象或网络连接对象，这种本身含有其他功能，顺带实现了上下文管理器协议的，使用类**】
  - 函数：使用`from contextlib import contextmanager` 的`contextmanager`装饰器 装饰自定义函数，自定义函数内部含有yield参数，即生成器函数，调用时使用with .. as .. ,yield后的对象是as后的对象【**实现一个单纯的上下文管理器对象，使用函数实现**】